
# Database Configuration
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/adamtechdb_2?createDatabaseIfNotExist=true

spring.application.name=adamtech

spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/adamtechdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

spring.datasource.username=root
spring.datasource.password=admin
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


spring.jpa.properties.hibernate.globally_quoted_identifiers=true
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

#spring.jpa.show-sql=false

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Add this to your existing application.properties
# JWT Configuration
app.jwt.secret=yourSuperSecretKeyThatIsAtLeast32BytesLong123456789!
app.jwt.expiration=86400000


# Server Configuration
server.servlet.context-path=/adamtech
server.port=8080

# JWT Configuration
app.jwtSecret=adamTechSecretKeyForJWTTokenGeneration2024VerySecureKey123456789
app.jwtExpirationInMs=86400000
app.jwtRefreshExpirationInMs=604800000

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001,https://*.vercel.app,https://*.netlify.app

# Logging Configuration
logging.level.za.co.admatech=INFO
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG

# WebSocket Configuration
spring.websocket.cors.allowed-origins=http://localhost:3000,http://localhost:3001

# Jackson Configuration (for JSON serialization)
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.serialization.write-dates-as-timestamps=false